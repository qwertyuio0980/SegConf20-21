package server;

public class CipherServerFiles {

	//fazer metodo de encriptar(recebe keystore, keystore-password, file a encriptar)
	public void encript(String keystore, String keystorePassword, String filePath) {
		
	}
	
	//fazer metodo de des-encriptar(recebe keystore, keystore-password, file a desencriptar)
	public void decript(String keystore, String keystorePassword, String filePath) {
		
	}
}